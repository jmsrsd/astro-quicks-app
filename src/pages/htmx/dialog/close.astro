---
import DialogActionButton from "./index.astro";

const { url } = Astro;

const { props } = Astro;

for (const key of [...url.searchParams.keys()]) {
  props[key] = url.searchParams.get(key) ?? props[key];
}
---

<DialogActionButton />
